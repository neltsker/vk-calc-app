/**
 * Interface of VK Connect Promise. Use `sendPromise` in latest version of
 * `@vkontakte/vk-connect` instead
 *
 * @deprecated
 */
declare const vkConnectPromise: {
    send: import("@vkontakte/vk-connect").VKConnectSendPromise;
    subscribe: import("@vkontakte/vk-connect").VKConnectSubscribeOrUnsubscribe;
    unsubscribe: (fn: import("@vkontakte/vk-connect").VKConnectSubscribeHandler) => void;
    isWebView: () => boolean;
    supports: (method: string) => boolean;
};
/**
 * Type of VK Connect Promise
 */
export declare type VKConnectPromise = typeof vkConnectPromise;
export default vkConnectPromise;
